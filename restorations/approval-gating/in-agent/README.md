# Restorations, Approval gating / In-agent

**Empty by design.** Real rebuild lives in [`../client-side/`](../client-side/) (restore hook from VCS, re-enable Auto Mode) and [`../server-side/`](../server-side/) (re-enable branch protection, audit bypass events, unfreeze).
