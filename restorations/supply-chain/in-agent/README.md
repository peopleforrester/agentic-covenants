# Restorations, Supply chain / In-agent

**Empty by design.** Real rebuild lives in [`../client-side/`](../client-side/) (runtime reinstall, MCP re-pinning, lockfile regen) and [`../server-side/`](../server-side/) (image rebuild and re-sign, SBOM regen, signing key rotation if exposed, force redeploy).
