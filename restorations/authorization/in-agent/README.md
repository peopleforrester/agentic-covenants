# Restorations — Authorization / In-agent

**Empty by design.** Real rebuild lives in [`../client-side/`](../client-side/) (restore hook config from VCS) and [`../server-side/`](../server-side/) (reapply RBAC/Kyverno/IAM from declarative source, audit drift).
