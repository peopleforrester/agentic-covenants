# Restorations — Blast radius / In-agent

**Empty by design.** Real rebuild lives in [`../client-side/`](../client-side/) (operator-host reimage, runtime reinstall) and [`../server-side/`](../server-side/) (backup restore, IaC redeploy, NetworkPolicy reapply).
