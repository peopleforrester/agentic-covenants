# Working notes / source material — kept local, not published
.notes/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
npm-debug.log*

# Secrets
.env
.env.*
!.env.example

# Build / output
dist/
build/
*.tar.gz
# NOTE: `site/` is deliberately NOT ignored. It holds the site generator source
# (build.py, deploy.sh), which is tracked. Only its output is ignored, below.

# Local Claude Code session history (do not push)
.claude-history/

# Imported context archive from claude.ai migration — local Claude Code context only, never publish (public repo).
claude-ai-context/

# Harvested session transcripts (landed here by the session-harvest tooling; not part of this repo).
transcripts/
_INBOX.md

# Generated site output; published to the gh-pages branch, not tracked on main.
site/dist/

# Deploy worktree; transient, never committed.
.gh-pages-worktree/
