# Interventions — Identity / In-agent

**Empty by design.** The agent that misuses identity will not voluntarily revoke it. There is no in-agent intervention for identity. Listed for completeness; the actual response lives in [`../client-side/`](../client-side/) (kill + delete credential) and [`../server-side/`](../server-side/) (revoke + disable + deny).

## Citation

No mapping; this cell does not exist as a control.
