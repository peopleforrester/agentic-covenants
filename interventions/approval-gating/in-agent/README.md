# Interventions — Approval gating / In-agent

**Empty by design.** A compromised approval surface cannot fix itself. Real intervention lives in [`../client-side/`](../client-side/) (deny-all hook, Auto Mode off) and [`../server-side/`](../server-side/) (branch protection lock, deployment freeze).
