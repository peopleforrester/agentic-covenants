# Interventions, Supply chain / In-agent

**Empty by design.** A compromised package or MCP server cannot be reliably told to stop using itself. Real intervention lives in [`../client-side/`](../client-side/) (allowlist removal, package quarantine, runtime pin) and [`../server-side/`](../server-side/) (registry delete, FQDN deny, force redeploy).
